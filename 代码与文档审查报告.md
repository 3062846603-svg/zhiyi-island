# 知忆岛代码与文档审查报告

审查日期：2026-09-05。范围：前后端源码、依赖与运行配置、数据库初始化脚本、README、接口文档、需求文档及前端/后端/AI 模块 Markdown 技术文档。未逐页审阅 Word 报告，也未进行完整渗透测试或依赖漏洞扫描。当前目录没有 `.git`，本次针对源码快照审查，无法比较历史提交。

## 已复现的问题（按优先级）

### 1. [P1] 笔记和知识库缺少对象归属校验

位置：`zhiyi-island-backend/src/main/java/com/example/zhiyiislandbackend/controller/NoteController.java:66`、`:94`、`:127`；同目录 `KnowledgeController.java:53`、`:105`、`:185`、`:200`。

更新接口虽取得当前用户 ID，但只记录日志，没有把 ID 传给更新服务；详情和关联列表按资源 ID 直接查询，Service/Mapper 也没有补充用户条件。用户 B 使用用户 A 的笔记、知识库 ID，读取详情、列出知识库笔记、修改标题均返回 HTTP 200 / 业务码 200。仅检查已登录无法隔离个人数据。

建议：在服务层统一校验资源所属用户，并使读写 SQL 同时约束资源 ID 和当前用户 ID；补充两个用户之间读、改、关联列表的拒绝访问测试。

### 2. [P1] 笔记正文存在存储型 XSS

位置：`zhiyi-island-frontend/src/views/notes/NoteDetailView.vue:306`；同目录 `NotesView.vue:307`、`:338`。

笔记内容原样保存，再通过 `v-html` 插入页面，未进行 HTML 安全清洗。实测保存带 `onerror` 的无害测试图片后，浏览器将图片替代文字从“未执行”改为“审查脚本已执行”，确认事件处理器执行。结合问题 1，其他登录用户可以篡改受害者笔记，受害者打开页面便会执行其脚本，威胁 sessionStorage 中的登录态及用户数据。

建议：在保存/展示边界使用成熟的 HTML 白名单清洗，移除事件属性与危险 URL 协议；富文本正常样式应保留。AI 输出转 HTML 的路径也需采用同一规则。

### 3. [P1] 忘记密码重置成功后旧 Token 仍有效

位置：`zhiyi-island-backend/src/main/java/com/example/zhiyiislandbackend/controller/UserController.java:314`；`service/impl/UserServiceImpl.java:404`。

普通修改密码会调用 Token 失效逻辑，但 `/api/user/password/reset` 只更新密码。实测测试用户重置密码成功后，重置前的 Token 请求 `/api/user/info` 仍返回 HTTP 200 / 业务码 200。如果重置是为收回被盗账户，原会话仍然能够访问账户。

建议：成功重置密码时失效该用户的全部 Token，并验证旧 Token 被拒绝、新密码可以重新登录。

### 4. [P1] 保存笔记会删除仍被保留的图片对象

位置：`zhiyi-island-backend/src/main/java/com/example/zhiyiislandbackend/service/impl/NoteServiceImpl.java:112`—`:121`。

更新时先删除全部旧图片记录和 MinIO 对象，再按请求中的 URL 重建记录。编辑页面会回传原有图片 URL，因此只修改标题也会删除仍要保留的文件。实测保存前图片 GET 为 200，再次保存同一 URL 后变为 404，笔记数据库仍保留一个图片链接。

建议：比较新旧 URL 集合，只删除被移除且归当前用户所有的对象；在数据库提交成功后处理文件清理。验证“仅改标题”“保留部分图片”“删除图片”三种情况。

### 5. [P1] 笔记可关联到其他用户的知识库

位置：`zhiyi-island-backend/src/main/java/com/example/zhiyiislandbackend/service/impl/NoteServiceImpl.java:48`、`:105`、`updateKnowledgeId` 方法。

创建、更新和更换关联时直接接受请求中的 `knowledgeId`，没有检查目标知识库归属。实测用户 B 在创建自己的笔记时提交 A 的知识库 ID，返回业务码 200，可污染 A 的知识库关联和统计。修复笔记本身的归属校验仍不足以解决此问题。

建议：每条关联写入路径都校验目标知识库存在且属于当前用户；知识条目的 `noteId` 关联也应校验来源笔记归属。

### 6. [P2] 邮箱验证码接口忽略验证结果

位置：`zhiyi-island-backend/src/main/java/com/example/zhiyiislandbackend/controller/UserController.java:234`—`:237`。

`verifyCode` 返回布尔值，但控制器没有检查，始终返回“验证成功”。实测未设置任何验证码，提交 `invalid-code` 仍返回 HTTP 200 / 业务码 200。此结论针对 `/email/verify`；注册、换绑和重置密码中的其他校验并未因此自动全部失效。

建议：返回 false 时给出业务错误；成功预验证和后续消费验证码的职责也需明确，避免提前删除有效验证码。

### 7. [P2] 编辑笔记无法清空知识库关联

位置：`zhiyi-island-backend/src/main/resources/mapper/NoteMapper.xml:40`。

Service 会把 `knowledgeId` 设为 null，但动态 SQL 只有非 null 才更新此列。实测笔记关联知识库 2，通过 PUT 提交 null 后再次 GET，关联仍为 2。与此同时服务可能按“已解除关联”更新统计，造成行为不一致。

建议：根据接口是整体更新还是部分更新，明确区分“不更新”和“更新为 null”，保证主动解除关联写入数据库。

### 8. [P2] 文档公开的用户名检查接口被鉴权拦截

位置：`zhiyi-island-backend/src/main/java/com/example/zhiyiislandbackend/config/SecurityConfig.java:61`；`接口文档.md:68`—`:70`。

接口文档声明 `/api/user/check-username` 无需认证，控制器也说明用于注册时检查，但 Security 白名单没有该路径。实测匿名请求返回 HTTP 403，未注册用户无法按文档调用。

建议：如果保留注册前检查功能，按 GET 方法放行该路径；否则同步移除公开接口承诺。

## 文档与运行配置差异

- README 快速开始没有完整列出 MinIO 的安装、凭据及 9005 端口配置，JWT 占位符的替换步骤也不完整。代码实际默认使用 local profile，并已有 `application-local.yml.example`；本机启动说明已单独补充。
- README 将 PostgreSQL 版本写为 `42.7.3`，该值实际是 JDBC 驱动版本。运行环境应分别记录数据库版本与驱动版本。
- README/技术文档使用 `bge-large-zh-v1.5`；实际主配置是 `BAAI/bge-m3`。AI 文档将知识提取模型写为 Qwen，主配置为 GLM-5，需统一，避免按不同配置生成不兼容的检索数据。
- README 声称 Flying Saucer 为 9.7.2，POM 实际为 9.1.22；POM 中 `poi` 5.2.5 与 `poi-ooxml` 5.4.0 混用。构建通过不能证明所有 DOCX 运行路径兼容，本次未把版本差异单独判定为已复现故障。
- README 末尾写 MIT，根目录 LICENSE 开头为“木兰宽松许可证，第2版”。需要项目维护者确认正确声明并统一文档。
- `init.sql` 的表使用 `IF NOT EXISTS`，但索引和触发器没有重入保护。重复执行实测在 `idx_user_username` 已存在时报错；该脚本应按首次初始化使用，升级应使用独立迁移。
- 前端 README 仍是 Vue 模板说明，未解释项目服务依赖、本地凭据和外部 AI/邮箱配置。

## 验证与当前运行状态

- `npm ci --no-audit --no-fund` 成功；`npm run build` 成功。单个主 JS 约 2.05 MB（gzip 约 685 kB），存在大包提示。
- `mvnw.cmd -B test package` 成功，编译 126 个 Java 源文件并产出可运行 JAR。Maven 明确输出 `No tests to run`；前端也没有 test 脚本。
- 初始化 PostgreSQL 16.15、pgvector 0.8.6，建立 10 张业务表；Redis PING 为 PONG；MinIO 健康检查为 200。
- 前端 5174、后端 8080、PostgreSQL 5432、Redis 6379、MinIO 9005/9006 已绑定本机回环地址。
- `stop-local.ps1` / `start-local.ps1` 已实测停止并重启全部服务；重启后演示登录返回 200，欢迎笔记仍存在，前端与 MinIO 健康检查均返回 200。
- API 验证了登录、笔记/知识库创建读写删除、图片上传、以上业务缺陷；浏览器验证了真实登录首页及 XSS 测试。临时审查账户与笔记已清理，保留演示账户和欢迎笔记。
- 原始 API 证据保存在 `.local/review-results.json`，不含 Token 或密码。本次探针脚本依赖一次性的临时测试账号，不是已建立的回归测试套件。
- SMTP 和 SiliconFlow 凭据未提供，未验证邮件发送、完整注册流程和远程 AI 结果。本地配置暂时关闭自动知识提取，避免每次保存笔记触发无效请求；手动 AI 功能仍需配置有效密钥。

本次修改限于 `.gitignore`、本地运行配置、启停脚本和说明文档，没有修改上述业务逻辑。审查发现尚未修复。
