# 知忆岛后端技术文档

## 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [核心模块详解](#核心模块详解)
   - [用户模块](#用户模块)
   - [笔记模块](#笔记模块)
   - [知识库模块](#知识库模块)
   - [AI模块](#ai模块)
   - [搜索模块](#搜索模块)
   - [导入模块](#导入模块)
   - [导出模块](#导出模块)
   - [通知模块](#通知模块)
   - [存储模块](#存储模块)
5. [安全架构](#安全架构)
6. [数据模型](#数据模型)
7. [API接口文档](#api接口文档)
8. [配置说明](#配置说明)

---

## 项目概述

知忆岛后端是一个基于 Spring Boot 3 的 RESTful API 服务，为前端提供用户认证、笔记管理、知识库管理、AI 摘要、文件导入导出等功能。

### 核心功能

- **用户认证**：JWT Token 认证，支持登录、注册、密码重置
- **笔记管理**：笔记的增删改查、分类、搜索、草稿管理
- **知识库管理**：知识库和知识条目的管理
- **AI 功能**：智能摘要生成、关键词提取、知识图谱生成、向量搜索
- **文件导入**：支持 PDF、DOCX、TXT、MD、EPUB 等格式导入
- **数据导出**：支持 JSON、Markdown、PDF 格式导出
- **通知系统**：系统通知的创建和管理

---

## 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.5.11 | 基础框架 |
| Spring Security | 6.x | 安全框架 |
| MyBatis | 3.0.3 | ORM 框架 |
| PostgreSQL | 42.7.3 | 数据库（含pgvector扩展） |
| Redis | - | Token 存储 |
| JWT (jjwt) | 0.12.6 | Token 生成与验证 |
| MinIO | 8.6.0 | 对象存储 |
| OpenPDF | 1.3.35 | PDF 生成 |
| Flying Saucer | 9.7.2 | HTML 转 PDF |
| Thymeleaf | - | 模板引擎 |
| Apache PDFBox | 3.0.1 | PDF 解析（导入） |
| Apache POI | 5.4.0 | Office 文档处理 |
| Lombok | - | 代码简化 |

### AI服务

| 模型 | 用途 | 说明 |
|------|------|------|
| Pro/zai-org/GLM-5 | 对话生成 | 主要对话模型 |
| Qwen/Qwen2.5-32B-Instruct | 知识提取 | 复杂推理任务 |
| BAAI/bge-large-zh-v1.5 | 向量嵌入 | 中文语义向量 |

---

## 项目结构

```
zhiyi-island-backend/
├── src/main/java/com/example/zhiyiislandbackend/
│   ├── common/               # 公共类
│   │   ├── constant/         # 常量定义
│   │   │   ├── BusinessConstants.java
│   │   │   ├── MessageConstants.java
│   │   │   └── ValidationConstants.java
│   │   └── result/           # 统一响应
│   │       └── Result.java
│   ├── config/               # 配置类
│   │   ├── MinioConfig.java
│   │   ├── RedisConfig.java
│   │   ├── SecurityConfig.java
│   │   └── WebConfig.java
│   ├── controller/           # 控制器层
│   │   ├── AiController.java
│   │   ├── ExportController.java
│   │   ├── FileController.java
│   │   ├── ImportController.java
│   │   ├── KnowledgeController.java
│   │   ├── NoteController.java
│   │   ├── NotificationController.java
│   │   ├── SearchController.java
│   │   └── UserController.java
│   ├── exception/            # 异常处理
│   │   ├── BusinessException.java
│   │   └── GlobalExceptionHandler.java
│   ├── filter/               # 过滤器
│   │   └── JwtAuthenticationFilter.java
│   ├── handler/              # 类型处理器
│   │   ├── BaseEnumTypeHandler.java
│   │   ├── BooleanEnumTypeHandler.java
│   │   ├── ImportStatusTypeHandler.java
│   │   ├── KnowledgeCategoryTypeHandler.java
│   │   ├── NoteCategoryTypeHandler.java
│   │   ├── NoteStatusTypeHandler.java
│   │   ├── NotificationTypeHandler.java
│   │   ├── UserStatusTypeHandler.java
│   │   └── VectorTypeHandler.java
│   ├── mapper/               # MyBatis Mapper 接口
│   │   ├── AiSummaryMapper.java
│   │   ├── ExportRecordMapper.java
│   │   ├── ImportRecordMapper.java
│   │   ├── KnowledgeItemMapper.java
│   │   ├── KnowledgeMapper.java
│   │   ├── NoteImageMapper.java
│   │   ├── NoteMapper.java
│   │   ├── NotificationMapper.java
│   │   ├── SearchHistoryMapper.java
│   │   └── UserMapper.java
│   ├── model/                # 模型类
│   │   ├── dto/              # 数据传输对象
│   │   │   ├── ai/           # AI模块DTO
│   │   │   │   ├── request/
│   │   │   │   └── response/
│   │   │   ├── export/       # 导出模块DTO
│   │   │   │   ├── request/
│   │   │   │   └── response/
│   │   │   ├── imports/      # 导入模块DTO
│   │   │   │   ├── request/
│   │   │   │   └── response/
│   │   │   ├── knowledge/    # 知识库模块DTO
│   │   │   │   ├── request/
│   │   │   │   └── response/
│   │   │   ├── note/         # 笔记模块DTO
│   │   │   │   ├── request/
│   │   │   │   └── response/
│   │   │   ├── notification/ # 通知模块DTO
│   │   │   │   └── response/
│   │   │   ├── search/       # 搜索模块DTO
│   │   │   │   └── response/
│   │   │   └── user/         # 用户模块DTO
│   │   │       ├── request/
│   │   │       └── response/
│   │   ├── entity/           # 实体类
│   │   │   ├── AiSummary.java
│   │   │   ├── ExportRecord.java
│   │   │   ├── ImportRecord.java
│   │   │   ├── Knowledge.java
│   │   │   ├── KnowledgeItem.java
│   │   │   ├── Note.java
│   │   │   ├── NoteImage.java
│   │   │   ├── Notification.java
│   │   │   ├── SearchHistory.java
│   │   │   └── User.java
│   │   └── enums/            # 枚举类
│   │       ├── BaseEnum.java
│   │       ├── BooleanEnum.java
│   │       ├── ImportStatusEnum.java
│   │       ├── KnowledgeCategoryEnum.java
│   │       ├── NoteCategoryEnum.java
│   │       ├── NoteStatusEnum.java
│   │       ├── NotificationTypeEnum.java
│   │       └── UserStatusEnum.java
│   ├── service/              # 服务层接口
│   │   ├── AiProviderService.java
│   │   ├── AiService.java
│   │   ├── CodeService.java
│   │   ├── DataTransferService.java
│   │   ├── FileService.java
│   │   ├── IdempotencyService.java
│   │   ├── KnowledgeService.java
│   │   ├── MailService.java
│   │   ├── NoteService.java
│   │   ├── NotificationService.java
│   │   ├── SearchHistoryService.java
│   │   ├── TokenService.java
│   │   └── UserService.java
│   ├── service/impl/         # 服务层实现
│   │   ├── AiProviderServiceImpl.java
│   │   ├── AiServiceImpl.java
│   │   ├── CodeServiceImpl.java
│   │   ├── DataTransferServiceImpl.java
│   │   ├── FileServiceImpl.java
│   │   ├── IdempotencyServiceImpl.java
│   │   ├── KnowledgeServiceImpl.java
│   │   ├── MailServiceImpl.java
│   │   ├── NoteServiceImpl.java
│   │   ├── NotificationServiceImpl.java
│   │   ├── SearchHistoryServiceImpl.java
│   │   ├── TokenServiceImpl.java
│   │   └── UserServiceImpl.java
│   ├── task/                 # 定时任务
│   │   └── StatisticsCalibrationTask.java
│   ├── util/                 # 工具类
│   │   ├── JwtUtil.java
│   │   ├── SecurityUtil.java
│   │   └── TestDataImporter.java
│   └── ZhiyiIslandApplication.java  # 启动类
├── src/main/resources/
│   ├── mapper/               # MyBatis XML 映射文件
│   ├── templates/            # Thymeleaf 模板
│   │   └── export-pdf.html   # PDF 导出模板
│   ├── db/                   # 数据库脚本
│   │   └── init.sql          # 初始化脚本
│   └── application.yml       # 应用配置
└── pom.xml                   # Maven 配置
```

---

## 核心模块详解

### 用户模块

**控制器**: `UserController.java`

**路由前缀**: `/api/user`

**功能描述**:
处理用户认证、信息管理和账户相关的 HTTP 请求。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | /register | 用户注册 | 否 |
| GET | /check-username | 检查用户名是否存在 | 否 |
| POST | /login | 用户登录 | 否 |
| POST | /login/code | 验证码登录 | 否 |
| POST | /logout | 用户登出 | 是 |
| GET | /info | 获取用户信息 | 是 |
| PUT | /password | 修改密码 | 是 |
| PUT | /password/by-code | 通过验证码修改密码 | 是 |
| POST | /password/verify | 验证密码 | 是 |
| POST | /email/verify | 验证邮箱 | 是 |
| PUT | /info | 更新用户信息 | 是 |
| PUT | /email | 换绑邮箱 | 是 |
| POST | /email/code | 发送验证码 | 否 |
| POST | /password/reset | 重置密码 | 否 |
| POST | /avatar | 上传头像 | 是 |
| DELETE | /account | 删除账户 | 是 |

#### 核心逻辑

```java
@PostMapping("/login")
public Result<LoginResponse> login(@Valid @RequestBody LoginRequest request) {
    LoginResponse response = userService.login(request);
    tokenService.saveToken(response.getToken(), response.getUserId());
    return Result.success("登录成功", response);
}

@PostMapping("/logout")
public Result<Void> logout(HttpServletRequest request) {
    String token = extractToken(request);
    if (token != null) {
        Long userId = jwtUtil.getUserIdFromToken(token);
        tokenService.invalidateAllTokensByUserId(userId);
    }
    return Result.success("登出成功", null);
}
```

---

### 笔记模块

**控制器**: `NoteController.java`

**路由前缀**: `/api/note`

**功能描述**:
处理笔记相关的 HTTP 请求，包括笔记的增删改查、搜索和草稿管理等功能。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | / | 创建笔记 | 是 |
| PUT | /{id} | 更新笔记 | 是 |
| DELETE | /{id} | 删除笔记 | 是 |
| GET | /{id} | 获取笔记详情 | 是 |
| GET | /list | 获取笔记列表 | 是 |
| GET | /knowledge/{knowledgeId} | 获取知识库关联笔记 | 是 |
| GET | /search | 搜索笔记 | 是 |
| PUT | /{id}/knowledge | 关联知识库 | 是 |
| GET | /count | 获取笔记数量 | 是 |
| POST | /{id}/summary | 生成笔记摘要 | 是 |
| GET | /drafts | 获取草稿列表 | 是 |
| GET | /drafts/count | 获取草稿数量 | 是 |
| PUT | /{id}/publish | 发布草稿 | 是 |
| GET | /categories | 获取分类列表 | 是 |

---

### 知识库模块

**控制器**: `KnowledgeController.java`

**路由前缀**: `/api/knowledge`

**功能描述**:
处理知识库和知识条目相关的 HTTP 请求。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | / | 创建知识库 | 是 |
| PUT | /{id} | 更新知识库 | 是 |
| DELETE | /{id} | 删除知识库 | 是 |
| GET | /count | 获取知识库数量 | 是 |
| GET | /item-count | 获取知识条目总数 | 是 |
| GET | /{id} | 获取知识库详情 | 是 |
| GET | /list | 获取知识库列表 | 是 |
| POST | /{knowledgeId}/item | 创建知识条目 | 是 |
| PUT | /item/{itemId} | 更新知识条目 | 是 |
| DELETE | /item/{itemId} | 删除知识条目 | 是 |
| GET | /{knowledgeId}/items | 获取知识条目列表 | 是 |
| GET | /note/{noteId}/items | 获取笔记关联的知识条目 | 是 |

---

### AI模块

**控制器**: `AiController.java`

**路由前缀**: `/api/ai`

**功能描述**:
处理 AI 相关的 HTTP 请求，包括摘要生成、关键词提取、知识图谱生成和对话。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | /summary | 生成 AI 摘要 | 是 |
| GET | /summary/history | 获取摘要历史 | 是 |
| DELETE | /summary/{id} | 删除摘要 | 是 |
| POST | /summary/{id}/save-to-note | 保存摘要到笔记 | 是 |
| POST | /keywords | 提取关键词 | 是 |
| GET | /knowledge-graph | 生成知识图谱 | 是 |
| POST | /chat | AI 对话 | 是 |

#### AI服务架构

```
AiController → AiServiceImpl → AiProviderServiceImpl → 硅基流动API
                                      ↓
                              Embeddings API → PostgreSQL pgvector
```

#### 核心服务接口

```java
public interface AiService {
    SummaryResponse generateSummary(Long userId, SummaryRequest request);
    List<SummaryResponse> getSummaryHistory(Long userId, Integer limit);
    void deleteSummary(Long userId, Long summaryId);
    void saveToNote(Long userId, Long summaryId);
    String extractKeywords(String content);
    String generateKnowledgeGraph(Long userId);
    String chat(Long userId, String question, String context);
    String semanticChat(Long userId, String question);
    KnowledgeExtractionResult extractKnowledge(String content);
    float[] generateEmbedding(String text);
    List<Note> searchByVector(Long userId, String query, int limit);
    List<Note> searchByVectorWithThreshold(Long userId, String query, double threshold, int limit);
    void batchGenerateEmbeddings(Long userId, int batchSize);
}
```

#### AI提供商服务

```java
public interface AiProviderService {
    String generateResponse(String prompt);
    String generateResponse(String prompt, String model);
    float[] generateEmbedding(String text);
}
```

---

### 搜索模块

**控制器**: `SearchController.java`

**路由前缀**: `/api/search`

**功能描述**:
处理搜索相关的 HTTP 请求，包括普通搜索、AI 搜索、智能搜索和语义化问答。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| GET | / | 普通搜索 | 是 |
| POST | /ai | AI 智能搜索 | 是 |
| POST | /intelligent | 智能搜索 | 是 |
| POST | /semantic-chat | 语义化问答 | 是 |
| GET | /history | 获取搜索历史 | 是 |
| DELETE | /history | 清空搜索历史 | 是 |
| DELETE | /history/{id} | 删除单条历史 | 是 |

#### 搜索模式

| 模式 | 说明 |
|------|------|
| 普通搜索 | 关键词匹配，支持笔记和知识库联合搜索 |
| AI智能搜索 | 语义扩展 + 向量相似度搜索 |
| 智能搜索 | AI理解查询意图，返回相关结果 |
| 语义化问答 | 理解自然语言问题，基于知识库回答 |

---

### 导入模块

**控制器**: `ImportController.java`

**路由前缀**: `/api/import`

**功能描述**:
处理文件导入相关的 HTTP 请求，支持多种文档格式导入。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | / | 导入文件 | 是 |
| GET | /history | 获取导入历史 | 是 |
| DELETE | /{id} | 删除导入记录 | 是 |

#### 支持的文件格式

| 格式 | 扩展名 | 说明 |
|------|--------|------|
| PDF | .pdf | PDF 文档 |
| Word | .docx | Word 文档 |
| 文本 | .txt | 纯文本文件 |
| Markdown | .md | Markdown 文件 |
| 电子书 | .epub | EPUB 电子书 |

---

### 导出模块

**控制器**: `ExportController.java`

**服务实现**: `DataTransferServiceImpl.java`

**路由前缀**: `/api/export`

**功能描述**:
处理数据导出相关的 HTTP 请求，支持多种格式导出。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | / | 导出数据 | 是 |
| GET | /history | 获取导出历史 | 是 |

#### 导出格式

| 格式 | 说明 | 实现方式 |
|------|------|----------|
| json | JSON 格式 | Jackson 序列化 |
| md | Markdown 格式 | 模板拼接 |
| pdf | PDF 格式 | OpenPDF + Flying Saucer + Thymeleaf |

#### PDF 导出实现

PDF 导出采用 **OpenPDF + Flying Saucer + Thymeleaf** 方案：

**核心代码**:

```java
private byte[] convertToPdf(Map<String, Object> exportData) {
    Context context = new Context();
    context.setVariable("exportTime", exportTime);
    context.setVariable("notes", notes);
    context.setVariable("knowledgeList", knowledgeViews);
    context.setVariable("aiSummaries", summaries);
    
    String htmlContent = templateEngine.process("export-pdf", context);
    
    ITextRenderer renderer = new ITextRenderer();
    renderer.getFontResolver().addFont(fontPath, BaseFont.IDENTITY_H, BaseFont.EMBEDDED);
    renderer.setDocumentFromString(htmlContent);
    renderer.layout();
    renderer.createPDF(outputStream);
    
    return outputStream.toByteArray();
}
```

**PDF 模板**: `src/main/resources/templates/export-pdf.html`

**字体支持**: 自动加载系统中文字体（simhei.ttf、simsun.ttc、msyh.ttc）

**PDF 特性**:
- 封面页（标题、导出时间）
- 分节显示（笔记、知识库、AI摘要）
- A4 纸张尺寸
- 中文字体嵌入

---

### 通知模块

**控制器**: `NotificationController.java`

**路由前缀**: `/api/notification`

**功能描述**:
处理通知相关的 HTTP 请求。

#### API 接口

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| GET | /list | 获取通知列表 | 是 |
| GET | /unread-count | 获取未读数量 | 是 |
| PUT | /{id}/read | 标记已读 | 是 |
| PUT | /read-all | 全部标记已读 | 是 |
| DELETE | /{id} | 删除通知 | 是 |

#### 通知类型

| 类型 | 值 | 说明 |
|------|------|------|
| SYSTEM | system | 系统通知 |
| NOTE | note | 笔记通知 |
| AI | ai | AI 通知 |
| KNOWLEDGE | knowledge | 知识库通知 |

---

## 安全架构

### JWT 认证流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Client    │────>│   Filter    │────>│  Controller │
└─────────────┘     └─────────────┘     └─────────────┘
                          │
                          ▼
                   ┌─────────────┐
                   │  JWT验证    │
                   │ TokenService│
                   └─────────────┘
                          │
                          ▼
                   ┌─────────────┐
                   │    Redis    │
                   │  Token存储  │
                   └─────────────┘
```

### JWT 认证过滤器

**文件**: `JwtAuthenticationFilter.java`

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                    HttpServletResponse response, 
                                    FilterChain filterChain) {
        String token = extractToken(request);
        
        if (token != null && jwtUtil.validateToken(token) && tokenService.isTokenValid(token)) {
            Long userId = jwtUtil.getUserIdFromToken(token);
            UsernamePasswordAuthenticationToken authentication =
                    new UsernamePasswordAuthenticationToken(userId, null, new ArrayList<>());
            SecurityContextHolder.getContext().setAuthentication(authentication);
        }
        
        filterChain.doFilter(request, response);
    }
}
```

### Token 服务

```java
public interface TokenService {
    void saveToken(String token, Long userId);
    void invalidateToken(String token);
    boolean isTokenValid(String token);
    Long getUserIdFromToken(String token);
    void invalidateAllTokensByUserId(Long userId);
}
```

### 白名单路径

| 路径 | 说明 |
|------|------|
| /api/user/register | 用户注册 |
| /api/user/login | 用户登录 |
| /api/user/check-username | 检查用户名 |
| /api/user/email/code | 发送验证码 |
| /api/user/password/reset | 重置密码 |

---

## 数据模型

### 实体类

#### User 用户实体

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 用户ID，主键自增 |
| username | String | 用户名，唯一标识 |
| password | String | 密码，加密存储 |
| email | String | 邮箱地址 |
| nickname | String | 昵称，显示名称 |
| avatar | String | 头像URL |
| phone | String | 手机号 |
| status | UserStatusEnum | 状态 |
| lastLoginTime | LocalDateTime | 最后登录时间 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Note 笔记实体

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID，主键自增 |
| userId | Long | 所属用户ID |
| knowledgeId | Long | 关联知识库ID |
| title | String | 笔记标题 |
| content | String | 笔记内容 |
| category | NoteCategoryEnum | 分类 |
| status | NoteStatusEnum | 状态（草稿/已发布） |
| wordCount | Integer | 字数 |
| source | String | 来源 |
| embedding | float[] | 向量嵌入（pgvector） |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Knowledge 知识库实体

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 知识库ID，主键自增 |
| userId | Long | 所属用户ID |
| title | String | 知识库标题 |
| description | String | 知识库描述 |
| category | String | 分类 |
| itemCount | Integer | 知识条目数量 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

---

## 配置说明

### application.yml

```yaml
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/zhiyi_island
    username: postgres
    password: ***REMOVED***
  
  data:
    redis:
      host: localhost
      port: 6379
  
  ai:
    siliconflow:
      base-url: https://api.siliconflow.cn/v1
      api-key: your-api-key
      model: Pro/zai-org/GLM-5
      knowledge-model: Pro/zai-org/GLM-5
      embedding-model: BAAI/bge-large-zh-v1.5
      options:
        temperature: 0.7
        max-tokens: 4096

jwt:
  secret: your-secret-key-at-least-256-bits-long
  expiration: 86400000

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-handlers-package: com.example.zhiyiislandbackend.handler

minio:
  endpoint: http://localhost:9005
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: zhiyi-island
```

### Maven 依赖

```xml
<dependencies>
    <!-- Spring Boot -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    
    <!-- Spring Security -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    
    <!-- MyBatis -->
    <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
        <version>3.0.3</version>
    </dependency>
    
    <!-- PostgreSQL -->
    <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>42.7.3</version>
    </dependency>
    
    <!-- JWT -->
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>0.12.6</version>
    </dependency>
    
    <!-- MinIO -->
    <dependency>
        <groupId>io.minio</groupId>
        <artifactId>minio</artifactId>
        <version>8.6.0</version>
    </dependency>
    
    <!-- OpenPDF -->
    <dependency>
        <groupId>com.github.librepdf</groupId>
        <artifactId>openpdf</artifactId>
        <version>1.3.35</version>
    </dependency>
    
    <!-- Flying Saucer -->
    <dependency>
        <groupId>org.xhtmlrenderer</groupId>
        <artifactId>flying-saucer-pdf-openpdf</artifactId>
        <version>9.7.2</version>
    </dependency>
    
    <!-- Thymeleaf -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>
    
    <!-- Apache PDFBox -->
    <dependency>
        <groupId>org.apache.pdfbox</groupId>
        <artifactId>pdfbox</artifactId>
        <version>3.0.1</version>
    </dependency>
    
    <!-- Apache POI -->
    <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi-ooxml</artifactId>
        <version>5.4.0</version>
    </dependency>
    
    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
    </dependency>
</dependencies>
```

---

## 通知触发场景

| 场景 | 通知类型 | 标题 | 内容模板 |
|------|----------|------|----------|
| 笔记创建成功 | NOTE | 笔记创建成功 | 您的新笔记《{标题}》已成功创建。 |
| 知识库创建成功 | KNOWLEDGE | 知识库创建成功 | 您的新知识库《{标题}》已成功创建。 |
| 知识条目添加成功 | KNOWLEDGE | 知识条目添加成功 | 您在《{知识库名}》中添加了新条目《{条目名}》。 |
| AI摘要生成成功 | AI | AI摘要生成完成 | 您的AI摘要《{标题}》已成功生成，共{字数}字。 |
| AI摘要生成失败 | AI | AI摘要生成失败 | 生成摘要《{标题}》失败，原因：{错误原因}。请稍后再试。 |
| 文件导入成功 | SYSTEM | 文件导入成功 | 文件《{文件名}》导入完成，共生成{数量}条笔记。 |
| 文件导入失败 | SYSTEM | 文件导入失败 | 文件《{文件名}》导入失败，原因：{错误原因}。 |

---

## 版本信息

- 文档版本: 2.0.0
- 更新日期: 2026-05-03
- Spring Boot 版本: 3.5.11
- Java 版本: 17
- 数据库: PostgreSQL（含pgvector扩展）
- AI服务: 硅基流动（SiliconFlow）
