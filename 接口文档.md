# 知忆岛 - API接口文档

## 1. 概述

### 1.1 基础信息
- 基础URL：`http://localhost:8080`
- 接口前缀：`/api`
- 认证方式：JWT Token（Bearer Token）
- 数据格式：JSON

### 1.2 统一响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 1.3 状态码说明
| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/Token无效 |
| 403 | 禁止访问 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |

### 1.4 认证说明
需要认证的接口需在请求头中携带Token：
```
Authorization: Bearer <token>
```

---

## 2. 用户模块 `/api/user`

### 2.1 用户注册
**POST** `/api/user/register`

**权限**：无需认证

**请求体**：
```json
{
  "username": "string (3-20字符，仅字母数字下划线)",
  "password": "string (6-20字符)",
  "email": "string (邮箱格式)",
  "nickname": "string (可选，最多20字符)",
  "code": "string (邮箱验证码)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "注册成功",
  "data": null
}
```

---

### 2.2 检查用户名是否存在
**GET** `/api/user/check-username`

**权限**：无需认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | String | 是 | 用户名 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": true
}
```

---

### 2.3 用户登录
**POST** `/api/user/login`

**权限**：无需认证

**请求体**：
```json
{
  "username": "string (用户名或邮箱)",
  "password": "string"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "string (JWT Token)",
    "userId": 1,
    "username": "string",
    "nickname": "string",
    "avatar": "string",
    "email": "string"
  }
}
```

---

### 2.4 验证码登录
**POST** `/api/user/login/code`

**权限**：无需认证

**请求体**：
```json
{
  "email": "string (邮箱)",
  "code": "string (验证码)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "string (JWT Token)",
    "userId": 1,
    "username": "string",
    "nickname": "string",
    "avatar": "string",
    "email": "string"
  }
}
```

---

### 2.5 用户登出
**POST** `/api/user/logout`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

---

### 2.6 获取用户信息
**GET** `/api/user/info`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "username": "string",
    "email": "string",
    "nickname": "string",
    "avatar": "string",
    "phone": "string",
    "status": "NORMAL",
    "lastLoginTime": "2024-01-01T00:00:00",
    "createTime": "2024-01-01T00:00:00"
  }
}
```

---

### 2.7 修改密码
**PUT** `/api/user/password`

**权限**：需要认证

**请求体**：
```json
{
  "oldPassword": "string",
  "newPassword": "string (6-20字符)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "密码修改成功，请重新登录",
  "data": null
}
```

---

### 2.8 通过验证码修改密码
**PUT** `/api/user/password/by-code`

**权限**：需要认证

**请求体**：
```json
{
  "email": "string (邮箱)",
  "code": "string (验证码)",
  "newPassword": "string (6-20字符)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "密码修改成功，请重新登录",
  "data": null
}
```

---

### 2.9 更新用户信息
**PUT** `/api/user/info`

**权限**：需要认证

**请求体**：
```json
{
  "nickname": "string (可选)",
  "avatar": "string (可选)",
  "phone": "string (可选)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "信息更新成功",
  "data": null
}
```

---

### 2.10 更新邮箱
**PUT** `/api/user/email`

**权限**：需要认证

**请求体**：
```json
{
  "email": "string (新邮箱)",
  "code": "string (验证码)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "邮箱绑定成功",
  "data": null
}
```

---

### 2.11 发送邮箱验证码
**POST** `/api/user/email/code`

**权限**：无需认证

**请求体**：
```json
{
  "email": "string (邮箱地址)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "验证码已发送",
  "data": null
}
```

---

### 2.12 重置密码（忘记密码）
**POST** `/api/user/password/reset`

**权限**：无需认证

**请求体**：
```json
{
  "email": "string",
  "code": "string (验证码)",
  "newPassword": "string (6-20字符)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "密码重置成功",
  "data": null
}
```

---

### 2.13 删除账户
**DELETE** `/api/user/account`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "账户已删除",
  "data": null
}
```

---

## 3. 笔记模块 `/api/note`

### 3.1 创建笔记
**POST** `/api/note`

**权限**：需要认证

**请求体**：
```json
{
  "title": "string (必填，最多200字符)",
  "content": "string (必填)",
  "category": "string (可选，分类名称)",
  "knowledgeId": 1
}
```

**响应**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "userId": 1,
    "knowledgeId": 1,
    "title": "string",
    "content": "string",
    "category": "学习笔记",
    "status": "PUBLISHED",
    "wordCount": 100,
    "source": "manual",
    "createTime": "2024-01-01T00:00:00",
    "updateTime": "2024-01-01T00:00:00"
  }
}
```

---

### 3.2 更新笔记
**PUT** `/api/note/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID |

**请求体**：同创建笔记

**响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": { /* 笔记信息 */ }
}
```

---

### 3.3 删除笔记
**DELETE** `/api/note/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

### 3.4 获取笔记详情
**GET** `/api/note/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": { /* 笔记信息 */ }
}
```

---

### 3.5 获取笔记列表
**GET** `/api/note/list`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | String | 否 | 分类筛选 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 笔记信息 */ }
  ]
}
```

---

### 3.6 获取知识库下的笔记列表
**GET** `/api/note/knowledge/{knowledgeId}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| knowledgeId | Long | 知识库ID |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 笔记信息 */ }
  ]
}
```

---

### 3.7 搜索笔记
**GET** `/api/note/search`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | String | 是 | 搜索关键词 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 笔记信息 */ }
  ]
}
```

---

### 3.8 更新笔记的知识库关联
**PUT** `/api/note/{id}/knowledge`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID |

**请求体**：
```json
{
  "knowledgeId": 1
}
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

---

### 3.9 获取笔记数量
**GET** `/api/note/count`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": 10
}
```

---

### 3.10 生成AI摘要
**POST** `/api/note/{id}/summary`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID |

**请求体**（可选）：
```json
{
  "style": "keypoints",
  "length": "medium"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "生成成功",
  "data": { /* 笔记信息（含摘要） */ }
}
```

---

### 3.11 获取草稿列表
**GET** `/api/note/drafts`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 草稿笔记信息 */ }
  ]
}
```

---

### 3.12 获取草稿数量
**GET** `/api/note/drafts/count`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": 3
}
```

---

### 3.13 发布草稿
**PUT** `/api/note/{id}/publish`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 笔记ID |

**响应**：
```json
{
  "code": 200,
  "message": "发布成功",
  "data": null
}
```

---

### 3.14 获取分类列表
**GET** `/api/note/categories`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": ["学习笔记", "工作记录", "生活随笔"]
}
```

---

## 4. 知识库模块 `/api/knowledge`

### 4.1 创建知识库
**POST** `/api/knowledge`

**权限**：需要认证

**请求体**：
```json
{
  "title": "string (必填)",
  "description": "string (可选)",
  "category": "string (可选)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "userId": 1,
    "title": "string",
    "description": "string",
    "category": "技术",
    "itemCount": 0,
    "noteCount": 0,
    "createTime": "2024-01-01T00:00:00",
    "updateTime": "2024-01-01T00:00:00"
  }
}
```

---

### 4.2 更新知识库
**PUT** `/api/knowledge/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 知识库ID |

**请求体**：同创建知识库

**响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": { /* 知识库信息 */ }
}
```

---

### 4.3 删除知识库
**DELETE** `/api/knowledge/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 知识库ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

### 4.4 获取知识库详情
**GET** `/api/knowledge/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 知识库ID |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "title": "string",
    "description": "string",
    "category": "技术",
    "itemCount": 5,
    "items": [
      {
        "id": 1,
        "knowledgeId": 1,
        "title": "string",
        "content": "string",
        "source": "string",
        "createTime": "2024-01-01T00:00:00",
        "updateTime": "2024-01-01T00:00:00"
      }
    ],
    "createTime": "2024-01-01T00:00:00",
    "updateTime": "2024-01-01T00:00:00"
  }
}
```

---

### 4.5 获取知识库列表
**GET** `/api/knowledge/list`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | String | 否 | 分类筛选 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 知识库信息 */ }
  ]
}
```

---

### 4.6 获取知识库数量
**GET** `/api/knowledge/count`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": 5
}
```

---

### 4.7 获取知识条目总数
**GET** `/api/knowledge/item-count`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": 20
}
```

---

### 4.8 创建知识条目
**POST** `/api/knowledge/{knowledgeId}/item`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| knowledgeId | Long | 知识库ID |

**请求体**：
```json
{
  "title": "string (必填)",
  "content": "string (必填)",
  "source": "string (可选)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": { /* 知识条目信息 */ }
}
```

---

### 4.9 更新知识条目
**PUT** `/api/knowledge/item/{itemId}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| itemId | Long | 知识条目ID |

**请求体**：同创建知识条目

**响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": { /* 知识条目信息 */ }
}
```

---

### 4.10 删除知识条目
**DELETE** `/api/knowledge/item/{itemId}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| itemId | Long | 知识条目ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

### 4.11 获取知识库下的所有条目
**GET** `/api/knowledge/{knowledgeId}/items`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| knowledgeId | Long | 知识库ID |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 知识条目信息 */ }
  ]
}
```

---

### 4.12 获取笔记关联的知识条目
**GET** `/api/knowledge/note/{noteId}/items`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| noteId | Long | 笔记ID |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 知识条目信息 */ }
  ]
}
```

---

## 5. 搜索模块 `/api/search`

### 5.1 普通搜索
**GET** `/api/search`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| query | String | 是 | 搜索关键词 |
| type | String | 否 | 搜索类型：all/note/knowledge，默认all |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "type": "note",
      "title": "string",
      "content": "string",
      "category": "string",
      "createTime": "2024-01-01T00:00:00"
    }
  ]
}
```

---

### 5.2 AI智能搜索
**POST** `/api/search/ai`

**权限**：需要认证

**请求体**：
```json
{
  "query": "string (搜索关键词)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "results": [
      { /* 搜索结果 */ }
    ],
    "expandedTerms": ["关键词1", "关键词2"],
    "total": 10
  }
}
```

---

### 5.3 智能搜索
**POST** `/api/search/intelligent`

**权限**：需要认证

**请求体**：
```json
{
  "query": "string (搜索关键词)"
}
```

**响应**：同AI智能搜索

---

### 5.4 语义化问答
**POST** `/api/search/semantic-chat`

**权限**：需要认证

**请求体**：
```json
{
  "question": "string (用户问题)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "answer": "string (AI回答)",
    "sourceType": "knowledge",
    "relatedContents": [
      { /* 相关内容 */ }
    ],
    "keywords": ["关键词1", "关键词2"],
    "intent": "definition",
    "confidence": 0.85
  }
}
```

---

### 5.5 获取搜索历史
**GET** `/api/search/history`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | int | 否 | 返回数量，默认10 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "keyword": "string",
      "searchType": "all",
      "createTime": "2024-01-01T00:00:00"
    }
  ]
}
```

---

### 5.6 清空搜索历史
**DELETE** `/api/search/history`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "清空成功",
  "data": null
}
```

---

### 5.7 删除单条搜索历史
**DELETE** `/api/search/history/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 历史记录ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

## 6. AI模块 `/api/ai`

### 6.1 生成AI摘要
**POST** `/api/ai/summary`

**权限**：需要认证

**请求体**：
```json
{
  "content": "string (必填，待摘要的内容)",
  "title": "string (可选)",
  "style": "string (可选，keypoints/narrative，默认keypoints)",
  "length": "string (可选，short/medium/long，默认medium)"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "生成成功",
  "data": {
    "id": 1,
    "title": "string",
    "summaryContent": "string",
    "summaryStyle": "keypoints",
    "summaryLength": "medium",
    "originalWordCount": 1000,
    "summaryWordCount": 200,
    "createTime": "2024-01-01T00:00:00"
  }
}
```

---

### 6.2 获取AI摘要历史
**GET** `/api/ai/summary/history`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | Integer | 否 | 返回数量，默认10 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 摘要信息 */ }
  ]
}
```

---

### 6.3 删除AI摘要
**DELETE** `/api/ai/summary/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 摘要ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

### 6.4 保存AI摘要到笔记
**POST** `/api/ai/summary/{id}/save-to-note`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 摘要ID |

**响应**：
```json
{
  "code": 200,
  "message": "已保存到笔记",
  "data": 1
}
```

---

### 6.5 提取关键词
**POST** `/api/ai/keywords`

**权限**：需要认证

**请求体**：
```
string (待提取关键词的内容)
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": "关键词1, 关键词2, 关键词3"
}
```

---

### 6.6 生成知识图谱
**GET** `/api/ai/knowledge-graph`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "nodes": [
      {"id": 1, "label": "概念名称", "level": 0}
    ],
    "edges": [
      {"from": 1, "to": 2}
    ]
  }
}
```

---

### 6.7 AI问答对话
**POST** `/api/ai/chat`

**权限**：需要认证

**请求体**：
```json
{
  "question": "string (用户问题)",
  "context": "string (上下文，可选)",
  "history": []
}
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "answer": "string (AI回答)",
    "context": "string (更新后的上下文)"
  }
}
```

---

## 7. 导入模块 `/api/import`

### 7.1 导入文件
**POST** `/api/import`

**权限**：需要认证

**请求类型**：`multipart/form-data`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 上传的文件（支持PDF、DOCX、TXT、MD、EPUB） |
| aiSummary | Boolean | 否 | 是否生成AI摘要 |

**响应**：
```json
{
  "code": 200,
  "message": "导入成功",
  "data": {
    "id": 1,
    "fileName": "string",
    "fileType": "pdf",
    "fileSize": 1024,
    "status": "SUCCESS",
    "noteCount": 5,
    "knowledgeCount": 3,
    "errorMessage": null,
    "createTime": "2024-01-01T00:00:00"
  }
}
```

---

### 7.2 获取导入历史
**GET** `/api/import/history`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { /* 导入记录信息 */ }
  ]
}
```

---

### 7.3 删除导入记录
**DELETE** `/api/import/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 记录ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

## 8. 导出模块 `/api/export`

### 8.1 导出数据
**POST** `/api/export`

**权限**：需要认证

**请求体**：
```json
{
  "notes": true,
  "knowledge": true,
  "aiSummaries": true,
  "format": "string (json/markdown/pdf)"
}
```

**参数说明**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| notes | Boolean | 否 | 是否导出笔记数据 |
| knowledge | Boolean | 否 | 是否导出知识库数据 |
| aiSummaries | Boolean | 否 | 是否导出AI摘要数据 |
| format | String | 是 | 导出格式：json、markdown、pdf |

**响应**：
文件下载（ResponseEntity<byte[]>）

**PDF 导出特性**：
- 使用 OpenPDF + Flying Saucer + Thymeleaf 生成
- 支持中文字体（黑体、宋体、微软雅黑）
- A4 纸张尺寸，带封面页和分节
- 自动过滤敏感字段（id、userId、embedding）

---

### 8.2 获取导出历史
**GET** `/api/export/history`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "userId": 1,
      "exportType": "notes,knowledge",
      "format": "pdf",
      "fileSize": 102400,
      "createTime": "2026-05-03T12:00:00"
    }
  ]
}
```

---

## 9. 通知模块 `/api/notification`

### 9.1 获取通知列表
**GET** `/api/notification/list`

**权限**：需要认证

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | Integer | 否 | 返回数量，默认10 |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "title": "string",
      "content": "string",
      "type": "SYSTEM",
      "isRead": false,
      "createTime": "2024-01-01T00:00:00"
    }
  ]
}
```

---

### 9.2 获取未读通知数量
**GET** `/api/notification/unread-count`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "count": 5
  }
}
```

---

### 9.3 标记通知已读
**PUT** `/api/notification/{id}/read`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 通知ID |

**响应**：
```json
{
  "code": 200,
  "message": "已标记已读",
  "data": null
}
```

---

### 9.4 标记所有通知已读
**PUT** `/api/notification/read-all`

**权限**：需要认证

**响应**：
```json
{
  "code": 200,
  "message": "已全部标记已读",
  "data": null
}
```

---

### 9.5 删除通知
**DELETE** `/api/notification/{id}`

**权限**：需要认证

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 通知ID |

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

## 10. 文件模块 `/api/file`

### 10.1 上传笔记图片
**POST** `/api/file/upload/note-image`

**权限**：需要认证

**请求类型**：`multipart/form-data`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 图片文件（最大10MB） |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "url": "string (图片URL)"
  }
}
```

---

### 10.2 上传头像
**POST** `/api/file/upload/avatar`

**权限**：需要认证

**请求类型**：`multipart/form-data`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 头像图片文件（最大2MB） |

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "url": "string (头像URL)"
  }
}
```

---

## 11. 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/Token无效/Token过期 |
| 403 | 禁止访问/权限不足 |
| 404 | 资源不存在 |
| 409 | 资源冲突（如用户名已存在） |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |

---

## 版本信息

- 文档版本: 2.1.0
- 更新日期: 2026-05-03
- API 版本: v1
