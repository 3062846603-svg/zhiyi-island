# 知忆岛前端技术文档

## 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [目录结构](#目录结构)
4. [页面详解](#页面详解)
   - [登录/注册页面](#登录注册页面)
   - [主布局](#主布局)
   - [首页](#首页)
   - [读书笔记模块](#读书笔记模块)
   - [知识库模块](#知识库模块)
   - [记忆搜索模块](#记忆搜索模块)
   - [个人中心模块](#个人中心模块)
   - [功能页面](#功能页面)
   - [设置页面](#设置页面)
5. [组件说明](#组件说明)
6. [状态管理](#状态管理)
7. [API接口](#api接口)

---

## 项目概述

知忆岛是一个基于 Vue 3 的智能读书笔记与知识管理应用，集成了 AI 摘要、知识图谱、记忆搜索等功能，帮助用户更好地管理和检索知识。

### 核心功能

- **读书笔记管理**：创建、编辑、分类、搜索笔记
- **知识库管理**：构建个人知识体系，支持知识条目管理
- **AI 智能摘要**：使用 AI 自动生成内容摘要
- **知识图谱**：可视化展示知识点之间的关联关系
- **记忆搜索**：普通搜索和 AI 智能搜索
- **文件导入**：支持 PDF、DOCX、TXT 等格式导入
- **数据导出**：支持 JSON、Markdown、PDF 格式导出

---

## 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue 3 | 3.x | 前端框架 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Vite | 5.x | 构建工具 |
| Element Plus | 2.x | UI 组件库 |
| Iconify | 3.x | 图标库 |
| Axios | 1.x | HTTP 客户端 |

---

## 目录结构

```
zhiyi-island-frontend/
├── src/
│   ├── api/                 # API 接口
│   │   ├── ai.js            # AI 接口
│   │   ├── export.js        # 导出接口
│   │   ├── file.js          # 文件接口
│   │   ├── import.js        # 导入接口
│   │   ├── knowledge.js     # 知识库接口
│   │   ├── note.js          # 笔记接口
│   │   ├── notification.js  # 通知接口
│   │   ├── search.js        # 搜索接口
│   │   └── user.js          # 用户接口
│   ├── components/          # 公共组件
│   │   ├── MultiImageUploader.vue  # 多图片上传组件
│   │   ├── Pagination.vue          # 分页组件
│   │   └── RichTextEditor.vue      # 富文本编辑器
│   ├── router/              # 路由配置
│   │   └── index.js
│   ├── stores/              # Pinia 状态管理
│   │   ├── ai.js            # AI 状态
│   │   ├── auth.js          # 认证状态
│   │   ├── knowledge.js     # 知识库状态
│   │   ├── note.js          # 笔记状态
│   │   ├── notification.js  # 通知状态
│   │   └── toast.js         # 提示状态
│   ├── utils/               # 工具函数
│   │   ├── request.js       # Axios 封装
│   │   └── validation.js    # 表单验证规则
│   ├── views/               # 页面组件
│   │   ├── drafts/          # 草稿箱模块
│   │   │   └── DraftView.vue
│   │   ├── home/            # 首页模块
│   │   │   ├── AISummaryView.vue
│   │   │   ├── AddNoteView.vue
│   │   │   ├── HomeView.vue
│   │   │   ├── ImportView.vue
│   │   │   └── KnowledgeGraphView.vue
│   │   ├── knowledge/       # 知识库模块
│   │   │   ├── KnowledgeDetailView.vue
│   │   │   └── KnowledgeView.vue
│   │   ├── layout/          # 布局组件
│   │   │   └── MainLayout.vue
│   │   ├── legal/           # 法律页面模块
│   │   │   ├── AboutView.vue
│   │   │   ├── OpenSourceLicenseView.vue
│   │   │   ├── PrivacyPolicyView.vue
│   │   │   └── UserAgreementView.vue
│   │   ├── login/           # 登录模块
│   │   │   └── LoginView.vue
│   │   ├── notes/           # 笔记模块
│   │   │   ├── NoteDetailView.vue
│   │   │   └── NotesView.vue
│   │   ├── notification/    # 通知中心模块
│   │   │   └── NotificationView.vue
│   │   ├── profile/         # 个人中心模块
│   │   │   ├── EmailBindView.vue
│   │   │   ├── ExportDataView.vue
│   │   │   ├── PasswordSettingsView.vue
│   │   │   ├── ProfileSettingsView.vue
│   │   │   └── ProfileView.vue
│   │   └── search/          # 搜索模块
│   │       └── SearchView.vue
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── public/                  # 静态资源
└── package.json             # 项目配置
```

---

## 页面详解

### 登录/注册页面

**文件路径**: `src/views/login/LoginView.vue`

**路由**: `/login`

**功能描述**:
提供用户登录、注册和找回密码三种表单模式，支持滑块验证和邮箱验证码。

**表单类型**:

| 表单 | 功能 |
|------|------|
| login | 用户登录，支持用户名/邮箱登录 |
| register | 用户注册，需要邮箱验证码 |
| forgot | 找回密码，通过邮箱重置密码 |

**核心功能**:

```javascript
// 登录处理
const handleLogin = async () => {
  const response = await authStore.login({
    username: loginForm.value.username,
    password: loginForm.value.password
  })
  if (response.code === 200) {
    router.push('/home')
  }
}

// 注册处理
const handleRegister = async () => {
  const response = await authStore.register({
    username: registerForm.value.username,
    email: registerForm.value.email,
    password: registerForm.value.password,
    code: registerForm.value.verifyCode
  })
}

// 发送验证码
const sendVerifyCode = async () => {
  const result = await userApi.sendEmailCode(registerForm.value.email)
}
```

**交互特性**:
- 滑块验证（向右滑动完成人机验证）
- 验证码倒计时（60秒）
- 记住用户名功能
- 密码显示/隐藏切换
- 表单切换动画

---

### 主布局

**文件路径**: `src/views/layout/MainLayout.vue`

**路由**: `/` (所有需要登录的页面父路由)

**功能描述**:
应用的主布局框架，包含侧边栏导航、顶部栏和通知面板。

**侧边栏菜单**:

| 菜单项 | 路由 | 图标 |
|--------|------|------|
| 首页 | /home | ri:home-4-line |
| 读书笔记 | /notes | ri:book-3-line |
| 知识库 | /knowledge | ri:brain-line |
| 记忆搜索 | /search | ri:search-line |
| 个人中心 | /profile | ri:user-3-line |

**通知系统**:

```javascript
// 加载通知数据
const loadNotifications = async () => {
  const [listResult, countResult] = await Promise.all([
    notificationApi.getList(10),
    notificationApi.getUnreadCount()
  ])
}

// 标记已读
const markAsRead = async (id) => {
  await notificationApi.markAsRead(id)
}

// 全部标记已读
const markAllAsRead = async () => {
  await notificationApi.markAllAsRead()
}
```

**布局结构**:
```
┌─────────────────────────────────────────────┐
│                  Header                      │
├──────────┬──────────────────────────────────┤
│          │                                  │
│ Sidebar  │           Content                │
│          │       (router-view)              │
│          │                                  │
└──────────┴──────────────────────────────────┘
```

---

### 首页

**文件路径**: `src/views/home/HomeView.vue`

**路由**: `/home`

**功能描述**:
展示用户统计数据、快捷操作入口和最近笔记列表。

**统计卡片**:

| 统计项 | 说明 | 跳转路由 |
|--------|------|----------|
| 读书笔记 | 笔记总数 | /notes |
| 知识条目 | 知识条目总数 | /knowledge |
| 本月阅读 | 本月新增笔记数 | /notes |

**快捷操作**:

| 操作 | 路由 | 颜色 |
|------|------|------|
| 添加笔记 | /home/add-note | #667eea |
| 导入文档 | /home/import | #764ba2 |
| AI 摘要 | /home/ai-summary | #f59e0b |
| 知识图谱 | /home/knowledge-graph | #10b981 |

**核心逻辑**:

```javascript
// 加载统计数据
const loadStats = async () => {
  const noteCount = await noteStore.getNoteCount()
  const knowledgeRes = await knowledgeApi.count()
  stats.value[0].value = noteCount || 0
  stats.value[1].value = knowledgeRes.data || 0
}

// 加载最近笔记
const loadRecentNotes = async () => {
  await noteStore.fetchNotes()
  recentNotes.value = noteStore.notes.slice(0, 3)
}
```

---

### 读书笔记模块

#### 笔记列表页

**文件路径**: `src/views/notes/NotesView.vue`

**路由**: `/notes`

**功能描述**:
展示用户的笔记列表，支持分类筛选和关键词搜索。

**筛选功能**:
- 分类筛选（从笔记数据中动态提取分类）
- 关键词搜索（标题、内容）

**核心逻辑**:

```javascript
// 过滤笔记
const filteredNotes = computed(() => {
  let result = noteStore.notes
  
  if (activeCategory.value !== '全部') {
    result = result.filter(note => note.category === activeCategory.value)
  }
  
  if (searchQuery.value.trim()) {
    const query = searchQuery.value.toLowerCase()
    result = result.filter(note => 
      note.title?.toLowerCase().includes(query) || 
      note.content?.toLowerCase().includes(query)
    )
  }
  
  return result
})
```

#### 笔记详情页

**文件路径**: `src/views/notes/NoteDetailView.vue`

**路由**: `/notes/:id`

**功能描述**:
展示笔记详细内容，支持 AI 摘要生成功能。

**功能特性**:
- 笔记内容展示
- AI 摘要生成
- 返回列表

#### 添加笔记页

**文件路径**: `src/views/home/AddNoteView.vue`

**路由**: `/home/add-note`

**功能描述**:
创建新的读书笔记，支持富文本编辑和图片上传。

**表单字段**:

| 字段 | 类型 | 必填 |
|------|------|------|
| title | 文本 | 是 |
| content | 富文本 | 是 |
| category | 选择 | 否 |
| images | 图片数组 | 否 |

**核心功能**:

```javascript
// 保存笔记
const saveNote = async (asDraft = false) => {
  const noteData = {
    title: noteForm.value.title,
    content: noteForm.value.content,
    category: noteForm.value.category,
    images: noteForm.value.images,
    status: asDraft ? 0 : 1
  }

  if (isEdit.value) {
    const result = await noteStore.updateNote(editNoteId.value, noteData)
  } else {
    const result = await noteStore.createNote(noteData)
  }
}
```

**功能特性**:
- 支持新建和编辑模式
- 富文本编辑器（支持多种格式）
- 多图片上传（最多9张）
- 草稿保存功能
- 分类选择

---

### 知识库模块

#### 知识库列表页

**文件路径**: `src/views/knowledge/KnowledgeView.vue`

**路由**: `/knowledge`

**功能描述**:
展示用户的知识库列表，支持网格/列表视图切换、分类筛选、搜索和操作菜单。

**视图模式**:
- grid: 网格视图
- list: 列表视图

**操作菜单**:
- 编辑
- 分享
- 删除

**核心逻辑**:

```javascript
// 过滤知识库
const filteredItems = computed(() => {
  let result = knowledgeStore.knowledgeList
  
  if (activeCategory.value !== '全部') {
    result = result.filter(item => item.category === activeCategory.value)
  }
  
  if (searchQuery.value.trim()) {
    const query = searchQuery.value.toLowerCase()
    result = result.filter(item => 
      item.title?.toLowerCase().includes(query) || 
      item.description?.toLowerCase().includes(query)
    )
  }
  
  return result
})

// 删除知识库
const handleDelete = async (item, event) => {
  await ElMessageBox.confirm(`确定要删除 "${item.title}" 吗？`, '提示')
  await knowledgeStore.deleteKnowledge(item.id)
}
```

#### 知识库详情页

**文件路径**: `src/views/knowledge/KnowledgeDetailView.vue`

**路由**: `/knowledge/:id`

**功能描述**:
展示知识库详情和知识条目列表，支持条目管理。

---

### 记忆搜索模块

#### 搜索页面

**文件路径**: `src/views/search/SearchView.vue`

**路由**: `/search`

**功能描述**:
提供普通搜索和 AI 智能搜索两种模式，支持搜索历史记录。

**搜索模式**:
- 普通搜索：关键词匹配
- AI 搜索：语义理解搜索

**筛选类型**:
- all: 全部
- note: 笔记
- knowledge: 知识

**核心逻辑**:

```javascript
// 普通搜索
const handleSearch = async () => {
  const result = await searchApi.search(searchQuery.value, activeFilter.value)
  searchResults.value = result.data.map(item => ({...}))
}

// AI 智能搜索
const handleAISearch = async () => {
  const result = await searchApi.aiSearch(searchQuery.value)
  searchResults.value = result.data.map(item => ({...}))
}
```

**推荐问题**:
- 什么是梯度下降算法？
- 如何提高模型泛化能力？
- 注意力机制与 RNN 的区别
- 常见的认知偏误有哪些？

---

### 个人中心模块

#### 个人中心首页

**文件路径**: `src/views/profile/ProfileView.vue`

**路由**: `/profile`

**功能描述**:
展示用户信息和设置入口。

**用户信息**:
- 头像
- 昵称/用户名
- 邮箱
- 注册日期

**设置分组**:

| 分组 | 设置项 |
|------|--------|
| 账户设置 | 个人资料、修改密码、换绑邮箱 |
| 数据管理 | 导入数据、导出数据 |
| 关于 | 关于我们 |

---

### 功能页面

#### AI 摘要页面

**文件路径**: `src/views/home/AISummaryView.vue`

**路由**: `/home/ai-summary`

**功能描述**:
使用 AI 生成文本内容的摘要，支持多种风格和长度选项。

**摘要选项**:

| 选项 | 可选值 |
|------|--------|
| length | short(简短)、medium(适中)、long(详细) |
| style | keypoints(要点)、detailed(详细)、outline(大纲) |
| language | chinese(中文)、english(英文) |

**核心功能**:

```javascript
// 生成摘要
const generateSummary = async () => {
  const result = await aiStore.generateSummary({
    content: inputContent.value,
    style: summaryOptions.value.style,
    length: summaryOptions.value.length
  })
  if (result) {
    summaryResult.value = result.summaryContent
    currentSummaryId.value = result.id
    toast.success('摘要生成成功')
  }
}

// 保存到笔记
const saveToNotes = async () => {
  const success = await aiStore.saveToNote(currentSummaryId.value)
  if (success) {
    toast.success('已保存到笔记')
  }
}
```

#### 文件导入页面

**文件路径**: `src/views/home/ImportView.vue`

**路由**: `/home/import`

**功能描述**:
支持导入 PDF、DOCX、TXT、MD、EPUB 格式的文件，自动提取内容生成笔记。

**支持格式**:
- PDF (.pdf)
- Word (.docx)
- 文本文件 (.txt)
- Markdown (.md)
- 电子书 (.epub)

**导入选项**:

| 选项 | 说明 |
|------|------|
| autoExtract | 自动提取内容 |
| aiSummary | AI 自动摘要 |
| autoCategory | 自动分类 |
| extractImages | 提取图片 |

**核心逻辑**:

```javascript
// 导入文件
const importFiles = async () => {
  for (const fileItem of filesToImport) {
    fileItem.status = 'processing'
    
    const result = await importApi.importFile(fileItem.file, {
      autoExtract: importOptions.value.autoExtract,
      aiSummary: importOptions.value.aiSummary,
      autoCategory: importOptions.value.autoCategory,
      extractImages: importOptions.value.extractImages
    })
    
    if (result.code === 200) {
      fileItem.status = 'success'
    } else {
      fileItem.status = 'error'
    }
  }
  
  // 显示结果
  if (successCount > 0) toast.success(`成功导入 ${successCount} 个文件`)
  if (errorCount > 0) toast.error(`导入失败 ${errorCount} 个文件`)
}
```

#### 知识图谱页面

**文件路径**: `src/views/home/KnowledgeGraphView.vue`

**路由**: `/home/knowledge-graph`

**功能描述**:
可视化展示知识点之间的关联关系，支持图谱和树形两种视图。

**视图模式**:
- graph: 图谱视图
- tree: 树形视图

**交互功能**:
- 缩放（放大/缩小/重置）
- 拖拽平移
- 节点选择

**核心逻辑**:

```javascript
// 加载知识图谱
const loadKnowledgeGraph = async () => {
  await noteStore.fetchNotes()
  const notes = noteStore.notes
  const content = notes.map(n => n.content).filter(Boolean).join('\n')
  
  const result = await aiStore.generateKnowledgeGraph(content)
  if (result && result.nodes) {
    nodes.value = result.nodes.map((node, index) => ({
      id: index + 1,
      label: node.label || node.name,
      x: node.x || 200 + (index % 4) * 150,
      y: node.y || 100 + Math.floor(index / 4) * 120,
      level: node.level || Math.floor(index / 4)
    }))
    edges.value = result.edges || []
  }
}
```

---

### 设置页面

#### 个人资料设置

**文件路径**: `src/views/profile/ProfileSettingsView.vue`

**路由**: `/profile/settings`

**功能描述**:
修改用户昵称、头像、手机号等信息。

**可修改字段**:
- nickname: 昵称
- avatar: 头像
- phone: 手机号

**头像上传**:

```javascript
const handleAvatarChange = async () => {
  const input = document.createElement('input')
  input.type = 'file'
  input.accept = 'image/*'
  input.onchange = async (e) => {
    const file = e.target.files[0]
    if (file.size > 5 * 1024 * 1024) {
      toast.warning('图片大小不能超过5MB')
      return
    }
    const result = await userApi.uploadAvatar(file)
    avatarUrl.value = result.data
  }
  input.click()
}
```

#### 修改密码

**文件路径**: `src/views/profile/PasswordSettingsView.vue`

**路由**: `/profile/password`

**功能描述**:
修改用户登录密码，修改成功后需要重新登录。

**表单字段**:
- currentPassword: 当前密码
- newPassword: 新密码
- confirmPassword: 确认密码

**密码规则**:
- 长度至少 6 位
- 新密码需确认一致

#### 导出数据

**文件路径**: `src/views/profile/ExportDataView.vue`

**路由**: `/profile/export`

**功能描述**:
支持导出笔记、知识库等数据，支持多种格式。

**导出选项**:
- notes: 笔记数据
- knowledge: 知识库（含知识条目）
- aiSummaries: AI 摘要

**导出格式**:
- json: JSON 格式
- md: Markdown 格式
- pdf: PDF 格式（带排版样式）

**核心逻辑**:

```javascript
const handleExport = async () => {
  const dataTypes = []
  if (exportOptions.value.notes) dataTypes.push('notes')
  if (exportOptions.value.knowledge) dataTypes.push('knowledge')
  if (exportOptions.value.aiSummaries) dataTypes.push('aiSummaries')
  
  const fileName = `知忆岛数据导出_${new Date().toISOString().slice(0, 10)}.${exportFormat.value}`
  
  await exportApi.downloadExport({
    notes: exportOptions.value.notes,
    knowledge: exportOptions.value.knowledge,
    aiSummaries: exportOptions.value.aiSummaries,
    format: exportFormat.value
  }, fileName)
}
```

**PDF 导出特性**:
- 封面页（标题、导出时间）
- 分节显示（笔记、知识库、AI摘要）
- 中文字体支持
- A4 纸张尺寸

#### 换绑邮箱

**文件路径**: `src/views/profile/EmailBindView.vue`

**路由**: `/profile/email`

**功能描述**:
更换绑定的邮箱地址，需要验证码验证。

---

## 组件说明

### 公共组件

| 组件 | 说明 |
|------|------|
| Icon | Iconify 图标组件 |
| ElMessageBox | Element Plus 消息框 |

### 页面组件结构

每个页面组件通常包含以下部分：

```vue
<script setup>
// 1. 导入依赖
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'

// 2. 定义状态
const loading = ref(false)
const dataList = ref([])

// 3. 定义方法
const loadData = async () => { ... }

// 4. 生命周期
onMounted(() => {
  loadData()
})
</script>

<template>
  <!-- 页面结构 -->
</template>

<style scoped>
/* 样式 */
</style>
```

---

## 状态管理

### Pinia Stores

| Store | 文件 | 说明 |
|-------|------|------|
| auth | stores/auth.js | 用户认证状态 |
| note | stores/note.js | 笔记数据状态 |
| knowledge | stores/knowledge.js | 知识库数据状态 |
| ai | stores/ai.js | AI 功能状态 |
| toast | stores/toast.js | 全局提示状态 |

### authStore

```javascript
// 状态
userInfo: null
token: localStorage.getItem('token')

// 方法
login(credentials)      // 登录
register(data)          // 注册
logout()               // 登出
fetchUserInfo()        // 获取用户信息
```

### noteStore

```javascript
// 状态
notes: []
currentNote: null
loading: false

// 方法
fetchNotes()           // 获取笔记列表
fetchNote(id)          // 获取笔记详情
createNote(data)       // 创建笔记
updateNote(id, data)   // 更新笔记
deleteNote(id)         // 删除笔记
searchNotes(query)     // 搜索笔记
getNoteCount()         // 获取笔记数量
```

### knowledgeStore

```javascript
// 状态
knowledgeList: []
currentKnowledge: null
loading: false

// 方法
fetchKnowledgeList()        // 获取知识库列表
createKnowledge(data)       // 创建知识库
updateKnowledge(id, data)   // 更新知识库
deleteKnowledge(id)         // 删除知识库
createItem(knowledgeId, data)  // 创建知识条目
```

---

## API接口

### 接口模块

| 模块 | 文件 | 说明 |
|------|------|------|
| user | api/user.js | 用户管理 |
| note | api/note.js | 笔记管理 |
| knowledge | api/knowledge.js | 知识库管理 |
| search | api/search.js | 搜索功能 |
| ai | api/ai.js | AI 功能 |
| import | api/import.js | 文件导入 |
| export | api/export.js | 数据导出 |
| notification | api/notification.js | 通知管理 |
| file | api/file.js | 文件管理 |

### 接口示例

```javascript
// 登录
POST /api/user/login
Body: { username, password }

// 注册
POST /api/user/register
Body: { username, email, password, code }

// 获取笔记列表
GET /api/note/list

// 创建笔记
POST /api/note
Body: { title, content, category, images }

// AI 摘要
POST /api/ai/summary
Body: { content, style, length }

// 搜索
GET /api/search?keyword=xxx&type=all

// AI 搜索
POST /api/search/ai
Body: { query }
```

---

## 路由守卫

```javascript
router.beforeEach((to) => {
  const token = localStorage.getItem('token')
  const requiresAuth = to.meta.requiresAuth !== false
  const isLoginPage = to.path === '/login'

  if (requiresAuth && !token && !isLoginPage) {
    return '/login'
  } else if (token && isLoginPage) {
    return '/home'
  }
})
```

---

## 弹窗提示规范

| 操作 | 成功弹窗 | 失败弹窗 |
|------|----------|----------|
| AI摘要生成 | "摘要生成成功" | "摘要生成失败" |
| 文件导入 | "成功导入 X 个文件" | "导入失败 X 个文件" |
| 笔记创建 | "保存成功" | "保存失败" |
| 密码修改 | "密码修改成功，请重新登录" | "密码修改失败" |
| 头像上传 | "头像上传成功" | "上传失败" |

---

## 版本信息

- 文档版本: 2.0.0
- 更新日期: 2026-05-03
- Vue 版本: 3.5
- Element Plus 版本: 2.x
- Vite 版本: 7.x
