<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zhiyiislandbackend.mapper.NotificationMapper">

    <resultMap id="BaseResultMap" type="com.example.zhiyiislandbackend.model.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="type" property="type"
                typeHandler="com.example.zhiyiislandbackend.handler.NotificationTypeHandler"/>
        <result column="is_read" property="isRead"
                typeHandler="com.example.zhiyiislandbackend.handler.BooleanEnumTypeHandler"/>
        <result column="read_time" property="readTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, title, content, type, is_read, read_time, create_time
    </sql>

    <insert id="insert" parameterType="com.example.zhiyiislandbackend.model.entity.Notification" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO notification (user_id, title, content, type, is_read, read_time, create_time)
        VALUES (#{userId}, #{title}, #{content},
        #{type, typeHandler=com.example.zhiyiislandbackend.handler.NotificationTypeHandler},
        #{isRead, typeHandler=com.example.zhiyiislandbackend.handler.BooleanEnumTypeHandler}, #{readTime},
        #{createTime})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM notification
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM notification
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*)
        FROM notification
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>

    <delete id="deleteById">
        DELETE FROM notification WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM notification WHERE user_id = #{userId}
    </delete>

</mapper>
