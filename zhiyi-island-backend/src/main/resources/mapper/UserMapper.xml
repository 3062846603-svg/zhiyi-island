<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zhiyiislandbackend.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.zhiyiislandbackend.model.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"
                typeHandler="com.example.zhiyiislandbackend.handler.UserStatusTypeHandler"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, email, nickname, avatar, phone, status, last_login_time, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.example.zhiyiislandbackend.model.entity.User" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO "user" (username, password, email, nickname, avatar, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{nickname}, #{avatar}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM "user"
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM "user"
        WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM "user"
        WHERE email = #{email}
    </select>

    <update id="updateById" parameterType="com.example.zhiyiislandbackend.model.entity.User">
        UPDATE "user"
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE "user"
        SET password = #{password}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateAvatar">
        UPDATE "user"
        SET avatar = #{avatar}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM "user" WHERE id = #{id}
    </delete>

</mapper>
